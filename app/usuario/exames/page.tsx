import React from 'react';
import { Container } from '@/components/dashboard/container';

const UserExamesIndex = () => {
	return (
		<Container>
			UserExamesIndex
		</Container>
	);
};

export default UserExamesIndex;